<div>

  <h3>Estas son las lecciones por su aprobación</h3>

  {{#each this.filteredLessons}}

    <div>
      <h5>{{this.topic}}</h5>
      <p>{{this.description}}</p>
      <p>{{this.bootcamp}}</p>

      {{!-- en handlebars cuando usamos if, solo podemos comparar si algo es verdadero o si algo existe --}}
      {{#if this.approved}}
        <p>Esta lección está aprobada!</p>
      {{else}}
        <p>Está no fue aprobada. Intenta de nuevo</p>
      {{/if}}
    </div>

    <hr>

  {{/each}}

</div>